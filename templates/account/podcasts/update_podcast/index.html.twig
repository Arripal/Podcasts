{% extends 'base.html.twig' %}

{% block title %}Hello Account/Podcasts/UpdatePodcastController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
 
    <h1>Modifier le podcast : {{ podcast.name }}</h1>
<pre>
    {{ dump(podcast) }}
</pre>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">Mettre à jour</button>
    {{ form_end(form) }}

    <a href="{{ path('app_account_podcasts') }}">Retour</a>
</div>
{% endblock %}
